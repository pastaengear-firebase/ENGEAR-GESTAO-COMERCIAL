
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Permissive rules: allow all authenticated reads and writes.
    // This is not secure for production environments with sensitive data.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
